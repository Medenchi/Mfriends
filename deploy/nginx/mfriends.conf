map $http_upgrade $connection_upgrade {
    default upgrade;
    '' close;
}

server {
    listen 80;
    server_name mfriends.denchy.cyou api.denchy.cyou ws.denchy.cyou cdn.denchy.cyou dev.denchy.cyou mail.denchy.cyou;

    location /.well-known/acme-challenge/ {
        root /var/www/certbot;
    }

    location / {
        return 301 https://$host$request_uri;
    }
}

server {
    listen 443 ssl http2;
    server_name mfriends.denchy.cyou;

    ssl_certificate /etc/letsencrypt/live/denchy.cyou/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/denchy.cyou/privkey.pem;
    ssl_protocols TLSv1.2 TLSv1.3;
    ssl_session_cache shared:MFRIENDS_SSL:10m;
    ssl_session_timeout 1d;

    root /opt/mfriends/frontend;
    index index.html;

    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;
    add_header Permissions-Policy "camera=(self), microphone=(self), geolocation=()" always;

    gzip on;
    gzip_comp_level 5;
    gzip_min_length 1024;
    gzip_types text/plain text/css application/json application/javascript image/svg+xml;

    location / {
        try_files $uri $uri/ /index.html;
        add_header Cache-Control "no-cache";
    }

    location ~* \.(?:css|js|svg|png|jpg|jpeg|webp|ico)$ {
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
    }
}

server {
    listen 443 ssl http2;
    server_name api.denchy.cyou;

    ssl_certificate /etc/letsencrypt/live/denchy.cyou/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/denchy.cyou/privkey.pem;

    add_header X-Frame-Options DENY always;
    add_header X-Content-Type-Options nosniff always;
    add_header Referrer-Policy strict-origin-when-cross-origin always;

    gzip on;
    gzip_types text/plain application/json;
    client_max_body_size 6m;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 60s;
    }
}

server {
    listen 443 ssl http2;
    server_name ws.denchy.cyou;

    ssl_certificate /etc/letsencrypt/live/denchy.cyou/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/denchy.cyou/privkey.pem;

    location /ws {
        proxy_pass http://127.0.0.1:8000/ws;
        proxy_http_version 1.1;
        proxy_set_header Upgrade $http_upgrade;
        proxy_set_header Connection $connection_upgrade;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
        proxy_read_timeout 3600s;
        proxy_send_timeout 3600s;
    }
}

server {
    listen 443 ssl http2;
    server_name cdn.denchy.cyou;

    ssl_certificate /etc/letsencrypt/live/denchy.cyou/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/denchy.cyou/privkey.pem;

    client_max_body_size 6m;

    location /uploads/ {
        alias /opt/mfriends/uploads/;
        types {
            image/jpeg jpg jpeg;
            image/png png;
            image/webp webp;
            audio/webm webm;
            audio/mpeg mp3;
        }
        default_type application/octet-stream;
        expires 1h;
        add_header Cache-Control "private, max-age=3600";
        add_header X-Content-Type-Options nosniff always;
    }

    location /static/ {
        alias /opt/mfriends/frontend/;
        expires 7d;
        add_header Cache-Control "public, max-age=604800, immutable";
    }
}

server {
    listen 443 ssl http2;
    server_name dev.denchy.cyou;

    ssl_certificate /etc/letsencrypt/live/denchy.cyou/fullchain.pem;
    ssl_certificate_key /etc/letsencrypt/live/denchy.cyou/privkey.pem;

    auth_basic "MFriends dev";
    auth_basic_user_file /etc/nginx/.mfriends-dev.htpasswd;

    location / {
        proxy_pass http://127.0.0.1:8000;
        proxy_http_version 1.1;
        proxy_set_header Host $host;
        proxy_set_header X-Real-IP $remote_addr;
        proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
        proxy_set_header X-Forwarded-Proto $scheme;
    }
}
